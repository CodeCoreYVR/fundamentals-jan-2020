// The callback passed to .ready() will be invoked when the DOM is ready for JS to execute
$(document).ready(function(e) {
  console.log('loaded')
})

// Below is a shorthand way of adding the ready event
$(function(e) {
  console.log('loaded')
})
